# Runtime moved

The production runtime has moved to the standalone repository:

`wp746/PP-Food-Runtime-001`

This embedded folder is deprecated and must not be installed or used as the production runtime. The standalone repository is the only production release source.
